name: Foundry Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable          # nightly o‘rniga stable — tavsiya qilinadi

      - name: Install dependencies
        run: forge install --no-commit     # --no-commit CI uchun muhim

      - name: Build
        run: forge build --sizes

      - name: Run tests
        run: forge test -v

      - name: Gas snapshot
        run: forge snapshot

      - name: Upload gas snapshot
        uses: actions/upload-artifact@v4
        with:
          name: gas-snapshot
          path: .gas-snapshot